extends layout 

block content
    section
        h1= `${err.message} - Status: ${err.status}`
        img(src='/static/img/sad.png')
        nav.nav
            a(href='/')
                span &larr;
                |  Back
